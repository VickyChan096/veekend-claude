# ── 本機建置才需要這個檔 ──────────────────────────────
# 正式部署走 GitHub Actions（.github/workflows/deploy.yml），
# 那邊會自動用 repo 名稱組出 baseURL，不讀本檔。

# GitHub Pages 子路徑。本機開發不用設（等同 '/'）；
# 要在本機重現正式建置時，複製成 .env.production 並填 '/<repo>/'（前後都要斜線），
# 然後跑 npm run generate:gh
#
# ⚠ 不要在 Git Bash 用 `NUXT_APP_BASE_URL=/x/ npm run generate` 這種行內寫法：
#   MSYS 會把值轉成 C:/Program Files/Git/x/，建置「成功」但每頁只產出 "Redirecting..."。
#   非用不可就前面加 MSYS_NO_PATHCONV=1。
NUXT_APP_BASE_URL=/

# GAS web app 的 /exec 網址（尚未接上，目前讀 app/assets/data/articles.json）
NUXT_PUBLIC_GAS_API_URL=
