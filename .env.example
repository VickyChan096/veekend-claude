# GitHub Pages 子路徑。本機開發不用設（等同 '/'）；
# 部署到 https://<user>.github.io/<repo>/ 時設成 '/<repo>/'（前後都要斜線）
#
# ⚠ 刻意不叫 NUXT_APP_BASE_URL——Nuxt 會把同名環境變數再套用一次，
#   結果每個頁面只 prerender 出 "Redirecting..."
VEEKEND_BASE_URL=/

# GAS web app 的 /exec 網址（Phase 4 尚未接上，目前讀 app/assets/data/db.json）
NUXT_PUBLIC_GAS_API_URL=
