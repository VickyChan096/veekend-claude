# GitHub Pages 子路徑。本機開發不用設（等同 '/'）；
# 部署到 https://<user>.github.io/<repo>/ 時設成 '/<repo>/'（前後都要斜線）
#
# ⚠ 在 Git Bash 用 `NUXT_APP_BASE_URL=/veekend/ npm run generate` 會踩到
#   MSYS 的 POSIX 路徑轉換，值被改寫成 C:/Program Files/Git/veekend/。
#   要嘛前面加 MSYS_NO_PATHCONV=1，要嘛就用本檔（--dotenv 讀檔不經過 shell）。
NUXT_APP_BASE_URL=/

# GAS web app 的 /exec 網址（尚未接上，目前讀 app/assets/data/articles.json）
NUXT_PUBLIC_GAS_API_URL=
